export interface ColorResponse {
  hex: string;
  rgb: string;
  hsl: string;
}

export interface RGB {
  r: number;
  g: number;
  b: number;
}

export interface HSL {
  h: number;
  s: number;
  l: number;
}
